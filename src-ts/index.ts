
export * from "./proto/eventsapis";

// import {ChannelCredentials} from "@grpc/grpc-js";
// import { EventsApisClient, PollEventsResponse } from "./proto/eventsapis";
//
// const client = new EventsApisClient("foo.var", ChannelCredentials.createInsecure());
// const stream = client.pollEvents({lastIdx: BigInt(0)});
// stream.on("data", (x: PollEventsResponse) => {
// });
